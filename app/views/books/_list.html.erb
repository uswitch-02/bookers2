<% @books.each do |book| %>
<!-- @post_images → post_images -->
  <h4>New book</h4>
  <% form_with model: @book do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>
<% end %>